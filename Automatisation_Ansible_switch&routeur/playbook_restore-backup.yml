---
  - name: Restauration du Backup de notre équipement "{{inventory_hostname}}"
    hosts: all
    gather_facts: false

    pre_tasks:
    - debug:
        msg: "Debut de la configuration"

    tasks:
    - name: Configuration du Backup
      ios_config:
        backup: yes
      register: config_output

    - name: Suppression des lignes current configuration
      lineinline:
        path: "./backup/{{inventory_hostname}}.config"
        regexp: 'Current configuration.*'
        state: absent

    post_tasks:
    - debug:
        msg: "Backup restauré"